import { Form, Input, Modal, Radio } from 'antd'
import React from 'react'
import { Toaster } from 'react-hot-toast'
import { TaskType } from '../../types/type'

const DetailTask = (task: TaskType) => {
    return (
        <div>
            
        </div>
    )
}

export default DetailTask